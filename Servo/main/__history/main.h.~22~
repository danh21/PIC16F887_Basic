#include <16F877A.h>
#device ADC=16

#FUSES NOWDT                    //No Watch Dog Timer
#FUSES NOBROWNOUT               //No brownout reset
#FUSES NOLVP                    //No low voltage prgming, B3(PIC16) or B5(PIC18) used for I/O

#use delay(crystal=20000000)

#define ctrlPulse PIN_B0

unsigned int16 T = 20000;
unsigned int16 minAngle = 0;
unsigned int16 maxAngle = 180;
unsigned int16 minCtrlPulse = 500;
unsigned int16 maxCtrlPulse = 2500;
unsigned int16 angle = 45;

//unsigned int16 Ton = (angle - minAngle) * (maxCtrlPulse - minCtrlPulse) / (maxAngle - minAngle) + minCtrlPulse;
unsigned int16 Ton = 45 * 2000 / 180 + 500;

#include <main.h>

int T_on = 1000;

void main() {  
   while(TRUE) {
      //TODO: User Code
      output_high(pulseCtrlServo);
      delay_us(T_on);
      output_low(pulseCtrlServo);
      delay_us(T - T_on);
   }
}

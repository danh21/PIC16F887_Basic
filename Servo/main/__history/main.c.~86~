#include <main.h>

void rotateServo(int T_on) {
   output_high(ctrlPulse);
   delay_us(833);
   output_low(ctrlPulse);
   delay_us(20000 - 833);
}

void main() {  

   while(TRUE) {
      //TODO: User Code
      //rotateServo(1500);
      output_high(ctrlPulse);
   delay_us(1000);
   output_low(ctrlPulse);
   delay_us(20000 - 1000);
   }
}

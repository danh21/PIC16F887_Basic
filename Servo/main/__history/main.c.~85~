#include <main.h>

void rotateServo(int T_on) {
   output_high(pulseCtrlServo);
   delay_us(833);
   output_low(pulseCtrlServo);
   delay_us(20000 - 833);
}

void main() {  

   while(TRUE) {
      //TODO: User Code
      rotateServo(1500);
   }
}

#include <main.h>

int T_on = 1000;

void main() {  
   while(TRUE) {
      //TODO: User Code
      output_high(pulseCtrlServo);
      delay_us(1000);
      output_low(pulseCtrlServo);
      delay_us(T - 1000);
   }
}

#include <main.h>

void main() {
   
   while(TRUE) {
      //TODO: User Code
      if (!Btn_rotate) {
         while (!Btn_rotate);
         Servo(1);
//!         rotateToMin(1);
         rotateToSec(5);
         Servo(0);
      }
   }

}

#include <main.h>

void main() {  
int T_on = 1000;
   while(TRUE) {
      //TODO: User Code
      output_high(pulseCtrlServo);
      delay_us(T_on);
      output_low(pulseCtrlServo);
      delay_us(T - T_on);
   }
}

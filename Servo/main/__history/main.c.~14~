#include <main.h>

void main() {
   
   while(TRUE) {
      //TODO: User Code
      if (!Btn_rotate) {
         while(!Btn_rotate);
         Motor(1);
         //rotateToMin(1);
         //rotateToSec(5);
         delay_ms(3000);
         Motor(0);
      }
   }

}

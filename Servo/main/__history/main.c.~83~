#include <main.h>

void rotateServo(int T_on) {
   output_high(pulseCtrlServo);
   delay_us(1000);
   output_low(pulseCtrlServo);
   delay_us(20000 - 1000);
}

void main() {  

   while(TRUE) {
      //TODO: User Code
      rotateServo(1000);
   }
}

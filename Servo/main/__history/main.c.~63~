#include <main.h>

void main() {
   
   while(TRUE) {
      //TODO: User Code
      output_high(pulseCtrlServo);
      delay_us(2500);
      output_low(pulseCtrlServo);
      delay_us(20000-2500);
   }
}

#include <main.h>

void rotateServo(int t) {
   output_high(ctrlPulse);
   delay_us(2500);
   output_low(ctrlPulse);
   delay_us(20000 - 2500);
}

void main() {  
unsigned int16 t = 2500;
   while(TRUE) {
      //rotateServo(2500);
      
      output_high(ctrlPulse);
      delay_us(Ton);
      output_low(ctrlPulse);
      delay_us(T - Ton);
   }
}

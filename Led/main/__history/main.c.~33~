#include <main.h>

void main() {
//!   char arr[3] = {0x55, 0x01, 0x80};
//!   int len = sizeof(arr) / sizeof(arr[0]);
//!   Data_Outs(arr, len);

   send_byte(0x55);
   delay_ms(1000);
   
   while(TRUE) {
      // on gradually, off gradually
      lightUp_fadeDown();
   }
      
}

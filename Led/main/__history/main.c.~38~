#include <main.h>

void main() {
   char arr[2] = {0x80, 0x55};
   
   send_bytes(arr);

   delay_ms(1000);
   Reset;
   
   while(TRUE) {
      // on gradually, off gradually
      lightUp_fadeDown();
   }
      
}

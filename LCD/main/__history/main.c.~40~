#include <main.h>

void main()
{
   lcd_init();
   lcd_clear();
   char chuoi1[10] = "CHELSEA";
   char chuoi2[10] = "TOTTENHAM";
   while(TRUE)
   {
      lcd_gotoxy(6,1);  // col 6, row 1
      lcd_puts(chuoi1);
      lcd_gotoxy(5,2);  // col 5, row 2
      lcd_puts(chuoi2);
   }

}
